{
  "type": "AdaptiveCard",
  "version": "1.4",
  "body": [
    {
      "type": "Container",
      "style": "default",
      "items": [
        {
          "type": "ColumnSet",
          "columns": [
            {
              "type": "Column",
              "width": "stretch",
              "items": [
                {
                  "type": "TextBlock",
                  "text": "🔧 **Pipeline Status**",
                  "weight": "Bolder",
                  "size": "Large",
                  "wrap": true,
                  "color": "Accent"
                },
                {
                  "type": "TextBlock",
                  "text": "@{triggerBody()?['title']}",
                  "weight": "Default",
                  "size": "Medium",
                  "wrap": true,
                  "color": "@{if(equals(triggerBody()?['theme_color'], '#DC3545'), 'Attention', if(equals(triggerBody()?['theme_color'], '#28A745'), 'Good', if(equals(triggerBody()?['theme_color'], '#FFC107'), 'Warning', 'Default')))}",
                  "spacing": "Small"
                }
              ],
              "verticalContentAlignment": "Center"
            },
            {
              "type": "Column",
              "width": "auto",
              "items": [
                {
                  "type": "TextBlock",
                  "text": "@{if(equals(triggerBody()?['theme_color'], '#28A745'), '✅', if(equals(triggerBody()?['theme_color'], '#DC3545'), '❌', if(equals(triggerBody()?['theme_color'], '#FFC107'), '⚠️', '🔄')))}",
                  "size": "ExtraLarge",
                  "horizontalAlignment": "Center"
                }
              ],
              "verticalContentAlignment": "Center"
            }
          ]
        }
      ]
    },
    {
      "type": "Container",
      "items": [
        {
          "type": "FactSet",
          "facts": [
            {
              "title": "Repository",
              "value": "@{triggerBody()?['repository']}"
            },
            {
              "title": "Branch",
              "value": "@{coalesce(triggerBody()?['branch'], triggerBody()?['branch_flow'])}"
            },
            {
              "title": "Pipeline",
              "value": "@{coalesce(triggerBody()?['build_name'], triggerBody()?['action'], 'Build')}"
            },
            {
              "title": "Triggered by",
              "value": "@{if(equals(triggerBody()?['author'], 'SYSTEM'), concat('System (', coalesce(triggerBody()?['event_category'], 'automated'), ')'), triggerBody()?['author'])}"
            },
            {
              "title": "Status",
              "value": "@{coalesce(triggerBody()?['build_status'], triggerBody()?['state'], triggerBody()?['action'])}"
            }
          ]
        }
      ],
      "spacing": "Medium"
    },
    {
      "type": "Container",
      "items": [
        {
          "type": "TextBlock",
          "text": "@{coalesce(triggerBody()?['subtitle'], triggerBody()?['description'])}",
          "wrap": true,
          "size": "Default",
          "isSubtle": true
        }
      ],
      "spacing": "Medium"
    },
    {
      "type": "Container",
      "style": "attention",
      "items": [
        {
          "type": "TextBlock",
          "text": "❌ **Failure Details**",
          "weight": "Bolder",
          "size": "Medium",
          "color": "Attention"
        },
        {
          "type": "TextBlock",
          "text": "@{coalesce(triggerBody()?['error_message'], triggerBody()?['failure_reason'], triggerBody()?['build_error'], 'Check the pipeline logs for detailed error information.')}",
          "wrap": true,
          "size": "Default",
          "spacing": "Small",
          "fontType": "Monospace"
        }
      ],
      "spacing": "Medium"
    }
  ],
  "actions": [
    {
      "type": "Action.OpenUrl",
      "title": "View Details",
      "url": "@{triggerBody()?['url']}"
    }
  ]
}